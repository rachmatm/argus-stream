# Copy this file to `.env` and uncomment/edit values as needed.
# Loaded by inference/frame_debug.py (and any future Python tooling).
# Real secrets should NOT be committed; this file is the safe template.

# Frame offset debugging - set to 1 to log decoded (x, y, channel) for
# sentinel offsets of each frame to stderr. 0 disables.
FRAME_DEBUG=0